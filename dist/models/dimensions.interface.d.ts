export declare class Dimensions {
    private _height;
    private _width;
    private _ratio;
    constructor(width: number, height: number);
    width: number;
    height: number;
    readonly ratio: number;
}
